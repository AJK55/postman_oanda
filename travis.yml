language: node_js
node_js:
  - "8.2.1"

install:
  - npm install newman

before_script:
  - node --version
  - npm --version
  - node_modules/newman --version

script:
  - node_modules/newman run tests/Oanda.postman_collection.json -e "Oanda - Demo.postman_environment.json"
  
  
